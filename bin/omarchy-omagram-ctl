#!/bin/bash

# omarchy:summary=Talk to the OmaGram bridge daemon
# omarchy:args=<status|ping|chats|messages|send|read|focus|refresh|login|loginPhone|code|password|api|reconnect|logout> [args]

source "$(dirname "$(readlink -f "$0")")/_common.sh"

exec "$(tg_node)" "$TG_DAEMON_DIR/ctl.js" "$@"
